apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: janus-proxy
  {{- with .Values.janus_proxy.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  rules:
    - host: {{ .Values.janus_proxy.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: janus-proxy
                port:
                  number: 80
